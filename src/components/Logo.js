import styled from 'styled-components';
import React from 'react';

const LogoWrapper = styled.div`
    color: #f37781;
    font-size: 4em;
    font-weight: 800;
`;

function Logo() {
  return <LogoWrapper>FLEX YOUR MEMORY</LogoWrapper>;
}

export default Logo;
